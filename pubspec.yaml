name: flutter_bytebank
description: Bytebank - Um projeto Alura agora no Flutter
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sqflite:
  path:
  http: ^0.13.5
  http_interceptor: ^1.0.2
  uuid:
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/bytebank_logo.png